# Data directory

Place your input video here as:

```text
data/full_lab.mp4
```

`scripts/run_lab.sh` looks for this path first (also accepts a path argument).

Videos and extracted frame folders are gitignored — they are too large for the repo.
Copy `full lab.mp4` from your machine or upload it on a cloud GPU box before running.
